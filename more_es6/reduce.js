//here i will generate a array numbers in all of sum.
const numbers = [1, 2, 3, 4, 5, 6];
const sumOfAllNumbers = numbers.reduce((initial, singleValue) => initial + singleValue, 0)
console.log(sumOfAllNumbers);
//initial that's mean first value by default
// need to be explore it again on doc.